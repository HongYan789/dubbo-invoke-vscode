{
  "name": "dubbo-invoke-generator",
  "displayName": "Dubbo Invoke Generator",
  "description": "Generate Dubbo invoke commands from interface methods",
  "publisher": "HongYan",
  "icon": "dubbo.png",
  "version": "2.1.0",
  "engines": {
    "vscode": "^1.74.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onLanguage:java"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "dubbo-invoke.generateInvokeCommand",
        "title": "Dubbo Invoke"
      },
      {
        "command": "dubbo-invoke.quickGenerateToClipboard",
        "title": "Quick Generate Dubbo Invoke to Clipboard"
      }
    ],
    "keybindings": [
      {
        "command": "dubbo-invoke.quickGenerateToClipboard",
        "key": "ctrl+i",
        "mac": "cmd+i",
        "when": "editorTextFocus && resourceExtname == '.java'"
      }
    ],
    "menus": {
      "editor/context": [
        {
          "command": "dubbo-invoke.generateInvokeCommand",
          "when": "editorTextFocus && resourceExtname == '.java'",
          "group": "1_modification"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/vscode": "^1.74.0",
    "@types/node": "16.x",
    "typescript": "^4.9.4"
  }
}